<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.3</version> <!-- Versión de Spring Boot compatible con Java 21 -->
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.example</groupId>
	<artifactId>citas-soap-service</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>citas-soap-service</name>
	<description>proyecto-Citas_Medicas</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version> <!-- Asegúrate de tener JDK 21 instalado -->
	</properties>
	<dependencies>
		<!-- Dependencia para Spring Data JPA (persistencia de datos con bases de datos) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<!-- Dependencia para Spring Web Services (para construir servicios SOAP) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web-services</artifactId>
		</dependency>

		<!-- Dependencia para Spring Boot DevTools (utilidades de desarrollo como recarga en caliente) -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<!-- Dependencia para la base de datos H2 (base de datos en memoria, ideal para desarrollo y pruebas) -->
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!-- Dependencia para Lombok (reduce el código boilerplate con anotaciones como @Data, @NoArgsConstructor) -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<!-- Dependencia para pruebas de Spring Boot -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Plugin del compilador de Maven: Configura el procesamiento de anotaciones para Lombok -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<!-- Plugin de Spring Boot Maven: Permite construir un JAR ejecutable y manejar dependencias -->
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<!-- Plugin para generar clases Java a partir del XSD (JAXB) -->
			<!-- Este plugin es crucial para que las clases del paquete com.example.citas.ws se generen automáticamente -->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>jaxb2-maven-plugin</artifactId>
				<version>2.5.0</version> <!-- Versión recomendada del plugin JAXB2 Maven -->
				<executions>
					<execution>
						<goals>
							<goal>xjc</goal> <!-- El objetivo 'xjc' genera las clases Java desde el XSD -->
						</goals>
					</execution>
				</executions>
				<configuration>
					<!-- Ruta al archivo XSD que define la estructura de tus mensajes SOAP -->
					<sources>
						<source>src/main/resources/xsd/citas.xsd</source>
					</sources>
					<!-- Directorio donde se guardarán las clases Java generadas.
					     Maven las incluirá automáticamente en el JAR final. -->
					<outputDirectory>${project.build.directory}/generated-sources/jaxb</outputDirectory>
					<clearOutputDir>false</clearOutputDir> <!-- No limpiar el directorio de salida antes de cada generación -->
					<!-- El paquete Java donde se colocarán todas las clases generadas (ej. Cita.java, RegistrarCitaRequest.java) -->
					<packageName>com.example.citas.ws</packageName>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
