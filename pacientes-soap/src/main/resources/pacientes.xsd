<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
           targetNamespace="http://cibertec.edu/pacientes"
           xmlns:tns="http://cibertec.edu/pacientes"
           elementFormDefault="qualified">

  <!-- Solicitud para registrar paciente -->
  <xs:element name="RegistrarPacienteRequest">
    <xs:complexType>
      <xs:sequence>
        <xs:element name="nombre" type="xs:string"/>
        <xs:element name="apellido" type="xs:string"/>
        <xs:element name="fechaNacimiento" type="xs:string"/>
        <xs:element name="telefono" type="xs:string"/>
        <xs:element name="email" type="xs:string"/>
        <xs:element name="direccion" type="xs:string"/>
      </xs:sequence>
    </xs:complexType>
  </xs:element>

  <!-- Respuesta del registro de paciente -->
  <xs:element name="RegistrarPacienteResponse">
    <xs:complexType>
      <xs:sequence>
        <xs:element name="resultado" type="xs:string"/>
      </xs:sequence>
    </xs:complexType>
  </xs:element>

</xs:schema>
